{% extends 'templates/base.twig' %}
{% use "templates/head.twig" %}

{% block content %}
	<link rel="stylesheet" href="{{ asset('css/categories.css') }}">
	<div class="content">
		{% include 'templates/menu.twig' %}

		<section class="courses">
			<h4>Categoria:
				{{ category.name }}</h4>
			<div>
				<h3>Decks em andamento</h3>
				<hr>
			</div>
			<div class="flex flex-md-row flex-sm-column flex-wrap justify-center">

				{% for deck in decks %}
					{% if deck.isPlaying == true %}

						<div>
							<a href="/baralhos/{{ deck.id }}/jogar/frente/audio" class="mdcr-sm-w-12 mdcr-md-w-4  flex mx-lg-1 mx-1 mdcr-card-a">

								<div class="mdcr-card mdcr-card-course-show">
									<img src="/{{ deck.thumbnail }}"/>
									<div class="mdcr-w-12">
										<div class="card-title">
											{{ deck.name }}</div>
									</div>
									<hr>
									{# <span>{{ category.description }}</span> #}
									<div>
										<span>Novos:
											<strong>{{ deck.totalNewCards }}</strong>
										</span>
										<span>A revisar:
											<strong>{{ deck.totalCardsToPlayAgain }}</strong>
										</span>
									</div>
								</div>
							</a>
							<a href="/baralhos/{{ deck.id }}/visualizar" class="manager">Gerenciar</a>
							<a href="#" class="manager" id="deck-listen-{{ deck.id }}">Ouvir</a>
						</div>
						<script>
							let audioElement{{ deck.id }} = document.getElementById('deck-listen-{{ deck.id }}')
							audioElement{{ deck.id }}.addEventListener('click', function (event) {
								event.preventDefault();
								console.log('jaaaaaaaaaava')
								let audioPlayElement = document.getElementById('audioplay-full');
								audioPlayElement.src = '/{{ deck.audiofile }}'
								let audioPlayDivElement = document.getElementById('audioplay-full-div');
								audioPlayDivElement.style.display = 'block';		
							})
						</script>
					{% endif %}
				{% endfor %}
			</div>
			<div>
				<h3>Decks novos</h3>
				<hr>
			</div>
			<div class="flex flex-md-row flex-sm-column flex-wrap justify-center">
				<div id="audioplay-full-div">
					<div id="audioplay-full-close"><div>x &nbsp;</div></div>
					<audio autoplay controls src="" id="audioplay-full">
						O seu navegador não suporta o elemento
						<code>audio</code>.
					</audio>
				</div>
				

				{% for deck in decks %}
					{% if deck.isPlaying == false %}
						<div>
							<a href="/baralhos/{{ deck.id }}/jogar/frente/audio" class="mdcr-sm-w-12 mdcr-md-w-4  flex mx-lg-1 mx-1 mdcr-card-a">

								<div class="mdcr-card mdcr-card-course-show">
									<img src="/{{ deck.thumbnail }}"/>
									<div class="mdcr-w-12">
										<div class="card-title">
											{{ deck.name }}</div>
									</div>
									<hr>
									{# <span>{{ category.description }}</span> #}
									<div>
										<span>Total de cards:
											<strong>{{ deck.totalNewCards }}</strong>
										</span>
									</div>
								</div>
							</a>
							<a href="/baralhos/{{ deck.id }}/visualizar" class="manager">Gerenciar</a>
							<a href="#" class="manager" id="deck-listen-{{ deck.id }}">Ouvir</a>

						</div>
						<script>
							let audioElement{{ deck.id }} = document.getElementById('deck-listen-{{ deck.id }}')
							audioElement{{ deck.id }}.addEventListener('click', function (event) {
								event.preventDefault();
								console.log('jaaaaaaaaaava')
								let audioPlayElement = document.getElementById('audioplay-full');
								audioPlayElement.src = '/{{ deck.audiofile }}'
								let audioPlayDivElement = document.getElementById('audioplay-full-div');
								audioPlayDivElement.style.display = 'block';		
							})
						</script>
					{% endif %}
				{% endfor %}
				
			</div>


		</section>
		<a class="add-new-float" href="/cursos/{{category.course_id}}/categorias/{{ category.id }}/baralhos/criar">+</a>
		<script>
		let closeBtn = document.getElementById('audioplay-full-close');
		closeBtn.addEventListener('click', function(event) {
			let audioPlayElement = document.getElementById('audioplay-full');
			audioPlayElement.src = ''
			let audioPlayDivElement = document.getElementById('audioplay-full-div');
			audioPlayDivElement.style.display = 'none';
		})
		</script>
	</div>
{% endblock %}
